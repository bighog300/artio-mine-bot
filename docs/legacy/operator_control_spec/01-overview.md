# Operator Control + Live Worker Console

Goal:
Enable up to 5 concurrent jobs with full operator control and live visibility.

Key capabilities:
- Max 5 concurrent jobs
- Pause / Resume / Cancel / Retry (cooperative)
- Worker visibility (who is doing what)
- Live job console (stage, item, progress)
- Structured event + log stream (SSE)
