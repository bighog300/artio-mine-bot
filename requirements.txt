# Production dependencies for Vercel serverless deployment.
# Playwright is intentionally excluded — it cannot run in a serverless
# environment.  Install the full package locally with:
#   pip install -e ".[dev]"

fastapi>=0.115.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
alembic>=1.13.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
openai>=1.30.0
tiktoken>=0.7.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
structlog>=24.1.0
tenacity>=8.3.0
